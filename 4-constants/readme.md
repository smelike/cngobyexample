
Go 支持字符(character)常数，string, boolean, 与数值。

const 声明一个常数值。

一个 const 声明放在一个 var 声明出现的地方。

常量表达式可以使用任意的数学精度。

一个数字常量没类型，除非它被赋予一个类型，例如，一个明确的转换。

一个数可被赋予一个类型，当它被程序上下文中需要时，例如：变量赋值或函数调用。

具体的案例，math.Sin 需要的是一个 float64。

